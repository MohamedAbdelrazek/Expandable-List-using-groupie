<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/item_expandable_header_root"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/item_expandable_header_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="8dp"
            android:textColor="?attr/colorAccent"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.5"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Group 1" />

        <ImageView
            android:id="@+id/item_expandable_header_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_toEndOf="@id/item_expandable_header_title"
            app:layout_constraintBottom_toBottomOf="@+id/item_expandable_header_title"
            app:layout_constraintStart_toEndOf="@+id/item_expandable_header_title"
            app:layout_constraintTop_toTopOf="@+id/item_expandable_header_title"
            app:srcCompat="@drawable/ic_keyboard_arrow_down_black_24dp" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>